import { Component, ChangeDetectionStrategy } from "@angular/core";

@Component({
    selector: 'theme-pack',
    styleUrls: ['./theme.component.scss'],
    template: '<span></span>',
    changeDetection: ChangeDetectionStrategy.OnPush
  })
  export class ThemepackComponent {
  
  }